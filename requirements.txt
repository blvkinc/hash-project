# Hash Monitor — runtime dependencies

# Web API + ASGI server
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0

# ORM + DB
sqlalchemy>=2.0,<3.0

# Filesystem event monitoring
watchdog>=3.0,<6.0

# LLM client (Ollama HTTP) + email helpers
requests>=2.31,<3.0

# Pydantic models (FastAPI dependency, pinned for clarity)
pydantic>=2.5,<3.0

# Cross-platform desktop notifications (Stage C)
plyer>=2.1,<3.0

# Fast hybrid hashing: XXH3 for rapid comparison, BLAKE3 for security-grade checks.
xxhash>=3.5,<4.0
blake3>=0.4,<2.0
